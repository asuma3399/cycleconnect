<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Photo Gallery</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div class="container">
    <h1>Photo Gallery</h1>
    <div class="photo-grid">
      <!-- 写真が登録されていない場合のプレースホルダー -->
      <div class="photo-item">
        <img src="placeholder.jpg" alt="No photo uploaded yet">
      </div>
      <!-- 実際の写真データをループで表示 -->
      <% @posts.each do |post| %>
        <div class="photo-item">
          <img src="<%= post.image_url %>" alt="<%= post.description %>">
        </div>
      <% end %>
    </div>
  </div>
</body>
</html>
