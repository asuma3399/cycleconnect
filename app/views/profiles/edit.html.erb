<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile</title>
  <%= stylesheet_link_tag 'profile_edit', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
  <div class="profile-container">
    <h1>Edit Profile</h1>
    <%= form_with model: @user, url: profile_path, local: true do |f| %>
      <div class="field">
        <%= f.label :username, "Username" %>
        <%= f.text_field :username, disabled: true, class: 'form-control' %>
      </div>
      <div class="field">
        <%= f.label :email, "Email" %>
        <%= f.email_field :email, disabled: true, class: 'form-control' %>
      </div>
      <div class="field">
        <%= f.label :bio, "Bio" %>
        <%= f.text_area :bio, class: 'edit-form-control' %>
      </div>
      <div class="field">
        <%= f.label :icon, "Profile Icon" %>
        <%= f.file_field :icon, class: 'edit-form-control' %>
      </div>
      <div class="actions">
        <%= f.submit "Save changes", class: 'profile-edit-save-btn' %>
      </div>
    <% end %>
  </div>
</body>
</html>
